services:
  - type: web
    name: ruta-superior-web
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start-render
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
